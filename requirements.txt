fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.0
argon2-cffi==23.1.0
python-multipart==0.0.20
pypdf==5.1.0
py7zr==1.1.3         # 7z archives (wheels for Linux + Windows/py3.13; skips cleanly if unimportable)
rarfile==4.2         # rar archives — needs `unar` in the image
python-docx==1.1.2
openpyxl==3.1.5
Pillow==11.0.0
pytesseract==0.3.13    # needs the `tesseract` binary in the image
ocrmypdf==16.7.0       # needs tesseract + ghostscript + qpdf (Docker only)
saq==0.22.4
psycopg[binary,pool]==3.2.3
aiogram==3.31.0
jinja2==3.1.5
itsdangerous==2.2.0    # signed CSRF + email-verification tokens for the web UI
aiosmtplib==3.0.2      # outbound email (account verification); only imported when SMTP_HOST is set
